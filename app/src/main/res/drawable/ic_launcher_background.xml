<!--
    Flat brand teal. The foreground carries the whole motif, so a patterned or
    gradient background would only fight it - and a flat fill compresses to a
    fraction of the size in every density bucket.
-->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <path
        android:fillColor="#006874"
        android:pathData="M0,0h108v108h-108z" />
</vector>
